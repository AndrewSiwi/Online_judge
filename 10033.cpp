#include <bits/stdc++.h>
#include <iostream>
#include <vector>
#include <string>

using namespace std;

void process()
{

}

int main(int argc, char** argv)
{
    vector<string> som;
    
    som.

    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int count;
    cin >> count;

    int i = 0;
    while(count--)
    {
        string instrs[1000] = { };

        cin >> instrs
    }

    return 0;
}